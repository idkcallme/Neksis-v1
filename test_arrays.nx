// Testing arrays or collections
fn main() -> Int {
    // Test if arrays are supported
    let numbers: [Int; 5] = [1, 2, 3, 4, 5];  // This syntax might fail
    println("Testing arrays");
    return 0;
}
