fn add_numbers(a: Int, b: Int) -> Int {
    return a + b;
}

fn main() -> Int {
    println("Testing functions...");
    
    let result: Int = add_numbers(5, 7);
    println("add_numbers(5, 7) =");
    println(result);
    
    return 0;
}
