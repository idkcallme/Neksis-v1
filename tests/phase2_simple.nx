// Simple Phase 2 Test
fn power(base: Int, exp: Int) -> Int {
    if exp == 0 {
        return 1;
    }
    return base * power(base, exp - 1);
}

fn fibonacci(n: Int) -> Int {
    if n <= 1 {
        return n;
    }
    return fibonacci(n - 1) + fibonacci(n - 2);
}

fn main() -> Int {
    println("🔥 PHASE 2: RECURSIVE FUNCTIONS 🔥");
    
    println("power(2, 5) =");
    println(power(2, 5));
    
    println("fibonacci(8) =");
    println(fibonacci(8));
    
    return 0;
}
